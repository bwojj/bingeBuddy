Implement two new screens — My Plan and Meditation — into the My Binge Buddy app at `frontend/` (Expo/React Native, expo-router), per `design_handoff_mbb_redesign/MY_PLAN_ADDENDUM.md`.

Read `MY_PLAN_ADDENDUM.md` in full first, and cross-reference `README.md` for the base design tokens (colors, type scale, spacing/shadow tokens, icon-mapping rules) — this is additive to that existing redesign, not a separate visual language. Also open `redesign/02-daily.html` (frames "My Plan" and "Meditation") and `redesign/mbb.css` (classes `.habit-card`, `.habit-week`, `.wd`, `.suggest-card`, `.ring-wrap`, `.playbtn`) as the pixel reference — they are static HTML mocks, not code to port; translate to native `StyleSheet` styles the same way the rest of the app already does.

Scope and ground rules:
1. **New screens, real functionality.** Unlike the base redesign (a restyle of existing screens), My Plan and Meditation are net-new. Build real state, not just static markup: habit list with per-habit streak + weekly completion, toggleable done-for-today checkboxes, an AI-suggestion card with approve/dismiss, and a working meditation timer with a duration picker.
2. **Add a 5th tab.** Find wherever the bottom tab bar and floating SOS button are currently implemented (check `frontend/app/(main)/_layout.jsx` and `tracker.jsx` per the base README's codebase map) and add "My Plan" as the 5th tab, in the same shared place — don't duplicate tab bar code per screen. Order: Dashboard, Progress, Journal, My Plan, Settings.
3. **Use the existing theme/token file** created by the base redesign (`frontend/constants/theme.js` or `Colors.js`) for all colors — do not hardcode new hex values. If that file doesn't exist yet in this codebase, create it from the README's Design Tokens section first.
4. **Reuse existing patterns**: follow the same `StyleSheet.create` conventions, icon library (`@expo/vector-icons` — map each Lucide name in the addendum to the closest Ionicons/MaterialCommunityIcons equivalent), fonts (Spectral for numbers/headings, Hanken Grotesk for UI text) already established by the base redesign.
5. **Data layer**: create `frontend/app/api/HabitAPI.js` (match the existing API module pattern, e.g. `UrgeAPI.js`/`JournalAPI.js`) covering habits, AI suggestions, and meditation sessions per the addendum's Data Model section. If there's no backend route for this yet, stub it with local mock data behind the same async call shape so swapping in a real endpoint later is a one-file change — flag this clearly in a code comment.
6. **Meditation timer**: implement a real countdown (not a static mock) — duration chip selection sets the timer length, play/pause controls it, and on completion it should update a meditation streak count and optionally mark the "Meditation" row on My Plan as done for today.
7. **No SOS button on My Plan** — that control stays only where the base redesign already places it; do not add it here.
8. Go screen by screen — My Plan first, then Meditation, then the shared tab bar update — and sanity-check each against the addendum's exact spec (streak colors, dashed suggestion card, week-dot states, ring/timer layout) rather than eyeballing.

Flag anything ambiguous (e.g. exact streak/reset logic, whether meditation completion should require the full duration or allow early-finish) instead of guessing silently.
