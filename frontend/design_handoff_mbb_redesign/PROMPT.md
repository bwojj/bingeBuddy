Implement the My Binge Buddy visual redesign documented in `design_handoff_mbb_redesign/README.md` into the real app at `frontend/` (Expo/React Native, expo-router).

Read the full README first — it has the exact color tokens, typography scale, per-screen visual specs, and a codebase map from design screens to real files. Do not read only this prompt.

Scope and ground rules:
1. This is a **restyle of existing, working screens** — every screen in the README's "Codebase Map" table already exists in `frontend/app/` and is functional. Do not rebuild logic, data fetching, navigation, or state management. Only change `StyleSheet.create` values, the minimal JSX structure needed to support new layout/hierarchy, icon names, and static copy/labels called out in the README.
2. The four `.html` files in the handoff folder plus `Logo.html` are **static web/HTML design references only** — screenshots-in-code, essentially. Never port HTML/CSS, never add a webview, never introduce Tailwind/NativeWind or any web styling library. Translate every visual spec into native `StyleSheet` styles on `View`/`Text`/`TouchableOpacity`/`ScrollView`/etc., matching how the surrounding file already writes styles.
3. **Centralize the color palette.** Today `#7e1f8c` (and incidental grays like `#888`, `#e8e3ea`, `#f2edf3`) are hardcoded inline across every screen's `StyleSheet.create`. Create `frontend/constants/theme.js` (or `Colors.js`, match whatever naming convention fits the project) exporting the full palette from the README's Design Tokens section, then replace every hardcoded color reference across all files with an import from it. Do this pass first — it touches every screen and de-risks the rest.
4. **Add the two Google Fonts** (`@expo-google-fonts/spectral`, `@expo-google-fonts/hanken-grotesk`) via `expo install`, load them with `useFonts` in `frontend/app/_layout.jsx` gated the same way `isAuthenticated === null` already gates `LoadingScreen`, and apply the serif weights to every heading/number callout and the sans weights to every UI/body/label text per the README's typography scale. Do not swap in a different font library or bundle raw font files manually.
5. **Keep the existing icon library** (`@expo/vector-icons` — `Ionicons` / `MaterialCommunityIcons`). The README's Lucide icon names are illustrative only; map each to the closest available Ionicons/MaterialCommunityIcons name (verify the name exists in the installed package version before using it) — do not add Lucide or any new icon package.
6. **Reuse existing working flows as-is**, only restyled: image picking (`expo-image-picker` via `HomeMotivation.jsx`), audio recording/playback, auth forms, journal API calls, AI coach chat. The redesign changes how these look, not how they work.
7. Go screen by screen in this order, verifying against the README's per-screen spec each time: Login → Sign Up → Main Cause → Motivation → Dashboard → Progress → Journal → AI Coach → SOS/Panic → Audio Recording → Settings → Profile Settings → Personalization → Privacy & Security. Update the shared floating SOS control and bottom tab bar (wherever they're actually implemented — check `_layout.jsx` and each main-loop screen) once, not per-screen, so the fix propagates.
8. Regenerate `frontend/assets/images/icon.png`, the Android adaptive-icon set, and `splash-icon.png` from the new brand mark provided in `assets/logo-icon-transparent.png` (or ask for higher-resolution exports if the target sizes need them) — keep each file's existing dimensions/shape requirements.
9. After each screen, sanity-check spacing/contrast against the README description (exact hex values, radii, shadow tokens) rather than eyeballing — the README is written to be precise enough to implement from without seeing the mock again.

If anything in the README is ambiguous or a real-app constraint conflicts with the mock (e.g. a native iOS/Android convention that the static HTML mock couldn't represent), flag it and propose the closest native equivalent rather than guessing silently.
